{
    "name": "novedades-ashley/ecommerce",
    "description": "Sistema de E-commerce Novedades Ashley con integración Stripe",
    "type": "project",
    "keywords": ["ecommerce", "stripe", "php", "payments"],
    "license": "MIT",
    "require": {
        "php": ">=7.4",
        "phpmailer/phpmailer": "^6.10",
        "stripe/stripe-php": "^17.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "autoload": {
        "psr-4": {
            "NovedadesAshley\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"if (!file_exists('logs/')) mkdir('logs/', 0755, true);\""
        ],
        "post-update-cmd": [
            "php -r \"if (!file_exists('logs/')) mkdir('logs/', 0755, true);\""
        ]
    }
}
